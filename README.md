# Currency Converter

A Java CLI tool to convert currencies using real-time rates.

## Setup
1. Get a free API key from [ExchangeRate-API](https://www.exchangerate-api.com/).
2. Set your API key as an environment variable named `EXCHANGE_RATE_API_KEY`.
3. Run the application.
